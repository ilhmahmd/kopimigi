// This file is intentionally minimal.
// Each app (pos, booth) creates its own Supabase client in src/lib/supabase.ts
// using their own Vite env vars.
export type { Database } from './database.types'
